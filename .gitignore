.env.local
.env*.local
node_modules
.vercel
.DS_Store
*.log
.env*
.bridge-keys/
.zcode/
trailer-output/

# Local agent settings and operational exports must never enter Git.
/.claude/
/.codex-remote-attachments/
/CLAUDE.md
/stats_temp*.json

# 작업 중 만든 임시 SQL/JSON. 저장소에 남을 이유가 없다.
scratch/
